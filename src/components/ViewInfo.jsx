import { NavLink } from "react-router-dom";

const ViewInfo = () => {
    return (
        <NavLink to='/info' className="block bg-black text-white text-center py-4 px-6">View Info</NavLink>
    );
}

export default ViewInfo;